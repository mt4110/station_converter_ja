# infra/terraform

Terraform / OpenTofu compatible skeleton.

- `modules/service_contract` keeps shared variable contracts
- `platforms/aws` / `gcp` / `azure` are split explicitly
- this scaffold is intentionally not production-complete
